{
  "name": "cpp-fluid-physics-sandbox",
  "version": "1.0.0",
  "description": "High-performance real-time particle and fluid physics simulation written in C++ and compiled to WebAssembly",
  "main": "src/web/app.js",
  "scripts": {
    "dev": "vite src/web --host",
    "build": "vite build src/web --outDir ../../dist --emptyOutDir",
    "compile": "mkdir -p src/web/public/wasm && emcc src/cpp/fluid_solver.cpp src/cpp/bindings.cpp -o src/web/public/wasm/fluid_sim.js -O3 -s WASM=1 -s ALLOW_MEMORY_GROWTH=1 -s MODULARIZE=1 -s EXPORT_NAME='createFluidSimModule' -s EXPORTED_RUNTIME_METHODS=\"['HEAPU8','HEAPF32','wasmMemory']\" --bind",
    "test": "node scripts/smoke.test.js"
  },
  "keywords": [
    "c++",
    "webassembly",
    "wasm",
    "physics-engine",
    "fluid-dynamics",
    "emscripten"
  ],
  "author": "Saksham Gupta",
  "license": "MIT",
  "devDependencies": {
    "jsdom": "^29.1.1",
    "playwright": "^1.61.1",
    "vite": "^5.2.0"
  }
}
